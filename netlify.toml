[build]
  command = "npm run build"
  publish = "dist"

[dev]
  framework = "vite"
  command = "npm run dev:vite"
  autoLaunch = false

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
